const fs = require('fs'); let app = fs.readFileSync('src/App.tsx', 'utf8'); app = app.replace('export default function App() {', 'export default function App() {\n  const [theme, setTheme] = useState(\'default\');\n'); app = app.replace('<div className="min-h-screen', '<div className={	heme- min-h-screen'); app = app.replace('<Dashboard onLogout', '<Dashboard theme={theme} setTheme={setTheme} onLogout'); fs.writeFileSync('src/App.tsx', app);
